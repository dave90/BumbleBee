SELECT c1."LAST NAME", c1."FIRST NAME" as F1, c2."FIRST NAME" as F2, c2."USERNAME"
FROM "./files/csv/username.csv" as c1, "./files/csv/username.csv" as c2
WHERE c1."LAST NAME" > c2."LAST NAME" AND c2."FIRST NAME" > c1."FIRST NAME"