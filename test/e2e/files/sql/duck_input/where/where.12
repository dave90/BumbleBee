SELECT c1."LAST NAME" as LAST_NAME, c1."FIRST NAME" FIRST_NAME, c1.USERNAME as USERNAME, c1.IDENTIFIER as IDENTIFIER
FROM './files/csv/username.csv' as c1
WHERE c1.USERNAME = 'admin' OR c1."LAST NAME" < 'M' AND c1."FIRST NAME" > 'A'