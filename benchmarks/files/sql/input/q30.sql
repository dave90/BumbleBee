%@sql
SELECT SEARCHENGINEID, CLIENTIP, COUNT(*) AS c, SUM(ISREFRESH) as sum, AVG(RESOLUTIONWIDTH) as avg
FROM "downloads/mini_hits.csv"
WHERE SEARCHPHRASE <> ""
GROUP BY SEARCHENGINEID, CLIENTIP
ORDER BY c DESC, CLIENTIP LIMIT 10
